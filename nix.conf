build-users-group = nixbld
trusted-users = root claude
experimental-features = nix-command flakes
sandbox = false
# Limit build parallelism to prevent OOM in containers
# 3 instances * 4 cores = 12 of 16 host cores, leaves room for the host
cores = 4
max-jobs = 1
